# Deposit Context

Scope:
- Handle native XTZ deposit into `VAULT_D` or `VAULT_S`.
- Show oracle APY for selected mode.
- Require wallet + SIWE style sign-in before write.

Out of scope:
- Splitter minting.
- AMM swaps.
- Portfolio aggregation.
