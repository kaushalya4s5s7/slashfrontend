# Split Context

Scope:
- Split slashDXTZ/slashSXTZ into PT + YT via `YieldSplitter.deposit(mode, amount)`.
- Handle ERC20 approve + split execution.

Out of scope:
- AMM swaps.
- Maturity redemption.
